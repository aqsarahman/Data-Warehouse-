1) download the Welcome.java file that has all the code
2) run the warehouse schema.sql file to create the warehouse in mysql
3) run the code and it will populate data into the schema of warehouse
4) run the lap.sql file to run OLAP queries 